/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '/LittleJobHelper',
  output: 'export',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;