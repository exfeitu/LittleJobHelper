# Findings & Decisions

## Requirements

- 先把首页普通“待办任务”区域实现为用户选定的方案 A。
- 用户所说的重点目标其实是可缩放时间轴上的任务卡片；普通列表完成后，为时间轴重新提供多套视觉方案。
- 本轮不直接实现时间轴新方案，等待用户选择。
- 用户已确认密度自适应方向：长时间段内按优先级决定信息保留，高优先级常驻完整卡，中优先级压缩，低优先级聚合。
- 卡片不能只向右顺延；在不移动真实时间锚点的前提下，应上下交错并允许围绕锚点左对齐、居中或右对齐。
- 2026-09-09 用户确认实施密度自适应时间轴方案，并要求完成后 push。
- 长时间段内任务过多时，显示层级必须由优先级决定，不能让所有任务维持同等大小。
- 时间轴上下区域需要更充分利用；允许卡片在保持时间锚点准确的前提下左右错位、上下交错。

## Research Findings

- 普通待办由 `components/todo-tree.tsx` 递归渲染，当前每个节点永久展示时间、关联记录、全部步骤、备注和标签，导致卡片高度不稳定。
- 普通待办当前同时显示批量选择框与状态圆点，语义和视觉负担重叠。
- 时间轴任务与工作记录共用 `components/day-timeline.tsx` 的 `.line-event-card`，任务通过 `.line-todo-card` 增量修饰。
- 时间轴支持连续缩放、周聚合和虚拟化；新视觉不能依赖固定卡片宽度或始终展示完整元数据。
- 当前时间轴任务卡仍固定高 `140px`，默认最窄 `72px`，悬停会扩为 `336px`；任务和事件的主要区别只有菱形锚点、虚线边框及“待办”文字。
- 当前 `positionedItems` 只记录同 lane 的最右边界，冲突时一律把后续卡片向右推；没有尝试另一侧、其他纵向位置或向左展开，因此会浪费轴线上下的空白区域。
- 当前 `trackHeight` 仍按固定 `LANE_HEIGHT=108` 和统一约 150px 卡高估算，无法从压缩后的任务卡片获益。
- 时间轴卡片宽度由布局层写入 `--card-width`，位置由 `--card-offset-x` 和 lane 偏移控制，因此新方案应以 CSS 信息折叠为主，避免改变定位模型。
- 普通待办整项当前承担 `role="button"`，批量 checkbox 会阻止冒泡；方案 A 需要把“展开详情”和“打开编辑”拆成明确、可键盘访问的操作。

## Technical Decisions

| Decision | Rationale |
|----------|-----------|
| 普通列表采用紧凑摘要行 + 单项展开详情 | 保持浏览密度，同时不丢失现有字段和编辑入口 |
| 批量选择改为显式模式并继续保留 | 用户已有批量操作流程；默认隐藏选择框可避免与状态圆点同时出现 |
| 状态点作为只读状态标识 | 避免未经确认改变状态操作流程 |
| 时间轴方案必须覆盖近、中、远三个缩放层级 | 这是可缩放时间轴卡片区别于普通卡片的核心约束 |
| 密度阈值以可见天数为稳定依据 | 1—2 天为低密度、超过 2 天至 10 天为中密度、超过 10 天为高密度；避免滚动时因局部任务计数变化导致卡片跳动 |
| 使用按优先级排序的二维装箱 | 时间锚点保持不动，只调整卡片矩形的左右展开方向、上下侧和离轴距离 |
| 密度按 `visibleDays` 而非原始 `scale` 判定 | 可见天数直接对应用户看到的时间跨度，阈值更容易测试和解释 |
| 远景低优先级任务按可见时间桶聚合 | 保留任务数量和时间分布，避免完全隐藏导致信息丢失 |
| 卡片布局排序优先处理高优先级任务 | 先为重要任务选择更好的 lane 和卡片位置，避免被低优先级项目挤到更深层 |

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| 安装依赖前没有 `node_modules`；安装后 `npm run dev` 仍无法启动 | 根因是 `scripts/dev.js` 在 Windows 直接 spawn `.bin/next` 无扩展名 shim；构建不受影响，本轮用等价 `npx next dev -p 3536 --webpack` 做视觉验证，不扩展修改范围 |
| ESLint 报 5 个 `day-timeline.tsx` Hook 依赖警告 | 均为改动前已有警告，本次普通待办改动未新增 lint 错误 |

## Resources

- `components/todo-tree.tsx`
- `components/day-timeline.tsx`
- `styles/components.css`
- `styles/timeline.css`
- `app/globals.css`

## Visual/Browser Findings

- 用户明确选择方案 A：无重复卡片外框、默认两行摘要、子任务用缩进和连接线、详情按需展开。
- 上一轮 PNG 预览能正确显示，因此时间轴候选方案继续使用 PNG 交付。
- 真实页面桌面宽度下，普通待办摘要行保持单行标题、单行元数据和右侧截止时间，整体高度明显低于旧卡片。
- 详情展开后能显示进度、步骤和备注；`aria-expanded` 从 false 正确变为 true。
- 批量模式开启后按钮 `aria-pressed=true`，状态圆点替换为选择框；退出/执行批量操作后会自动关闭模式。
- 360px 视口下页面 `scrollWidth` 与 `clientWidth` 均为 354px，无横向溢出；任务行宽 295px，展开详情宽 241px，均位于视口内。
- 时间轴方案 A“分级浮签”：1 天显示完整信息，7 天收为双行卡，30 天仅保留短标题和优先级；与现有点锚定定位模型最兼容。
- 时间轴方案 B“任务旗帜”：竖向占用更小且与工作记录差异最大，但视觉风格更强，密集时可能显得装饰性偏重。
- 时间轴方案 C“任务区间带”：直接表达开始到截止并把进度编码为填充长度，信息价值最高；实现时需改变当前待办固定 30 分钟宽的布局语义，无开始时间的任务还需退化为点标记。
- 真实页面默认“7天”视图命中中密度档：高优先级任务渲染为 174×72px 完整卡，时间轴总高保持 480px，卡片与锚点连接正确。
- 中密度卡片已从旧 140px 固定高度降至 72px；优先级色稳定映射为红/橙/灰绿，任务与工作记录的视觉身份更明确。
- 使用同日 7 个任务验证中密度时，降级结果正确：2 个高优先级完整卡、1 个中优先级单行卡、4 个低优先级聚合为 `+4`。
- 首轮密集截图发现两张高优先级完整卡落在相同坐标；根因是布局函数选出的 `side` 未传回渲染层。补齐映射后，同日四组卡片分别占用轴上近位、轴下近位、轴上远位、轴下远位，矩形不再重叠。
- 修正后的中密度几何结果为：高优先级完整卡 174×72px，中优先级紧凑卡 136×42px，低优先级聚合点 54×30px；轴线上下区域均被利用。
- 会话刷新后本地服务短暂中断，重启后浏览器将先前错误页拦截为受限导航，无法继续补录三档截图；完整密度规则和碰撞行为由纯函数测试覆盖，已有中密度真实页面结果保留为视觉依据。
